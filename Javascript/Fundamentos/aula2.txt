let: criar uma váriavel
const: criar uma constante

Strings:

No JavaScript temos bibliotecas e métodos nativos para trabalhar com as variáveis dinâmicamente tipadas
.replace("Y", "y") //troca uma caractere por outro

// Concatenação: Não usar o "+" para concatenar um texto com variáveis, pois existe a possibilidade do resultado ser diferente devido a tipagem dinâmica 

Números:
Number.isIntenger //verifica se um número é inteiro ou não inteiro
resultado = (true/false)
.toFixed(2) formatar em 2 casas decimais

NaN (Not a Number)

O JavaScript automáticamente determina se uma variável do tipo numérica não inteira precisa de alta precisão

ATENÇÃO!!!
obsoleto:
var //criação de variáveis
== // igual a
!= // diferente de

substituir por
let //criação de variáveis
=== //idêntico alta
!== //não idêntico

ATENÇÃO!!!
No JavaScript podemos omitir as chaves nas estruturas de controle, porém só uma linha de código é processada. CUIDADO!
